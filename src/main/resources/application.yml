spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yash
    username: yash
    password: yash
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    org:
      springframework: INFO